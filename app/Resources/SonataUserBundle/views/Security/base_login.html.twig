<!DOCTYPE html>
<html>
<head>

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Atlas des Agricultures du Monde | {{ 'title_user_authentication'|trans({}, 'SonataUserBundle') }}</title>

    {% block stylesheets %}
        {% stylesheets output="css/compiled/register.css" filter="cssrewrite"
        'bundles/admcore/css/normalize.css'
        'bundles/admcore/css/bootstrap.min.css'
        'bundles/admcore/css/style.css'
        'bundles/admcore/css/animate.css'
        'bundles/admcore/font-awesome/css/font-awesome.css'
        %}
        <link rel="stylesheet" href="{{ asset_url }}" />
        {% endstylesheets %}
    {% endblock %}

</head>

<body class="gray-bg">
    {% if error %}
        <div class="alert alert-danger alert-error">{{ error|trans({}, 'FOSUserBundle') }}</div>
    {% endif %}

    <div class="loginColumns animated fadeInDown">
        <div class="row">

            <div class="col-md-6">
                <h2 class="font-bold">Atlas des Agricultures du Monde</h2>

                <p>
                    Perfectly designed and precisely prepared admin theme with over 50 pages with extra new web app views.
                </p>

                <p>
                    Lorem Ipsum is simply dummy text of the printing and typesetting industry. Lorem Ipsum has been the industry's standard dummy text ever since the 1500s.
                </p>

                <p>
                    When an unknown printer took a galley of type and scrambled it to make a type specimen book.
                </p>

                <p>
                    <small>It has survived not only five centuries, but also the leap into electronic typesetting, remaining essentially unchanged.</small>
                </p>

            </div>
            <div class="col-md-6">
                <div class="ibox-content">
                    <form action="{{ path("fos_user_security_check") }}" method="post" role="form" class="m-t">
                        <input type="hidden" name="_csrf_token" value="{{ csrf_token }}"/>
                        <div class="form-group">
                            <input type="text" class="form-control" placeholder="{{ 'security.login.username'|trans({}, 'FOSUserBundle') }}" name="_username" value="{{ last_username }}"
                            required="required">
                        </div>

                        <div class="form-group">
                            <input type="password" class="form-control" placeholder="{{ 'security.login.password'|trans({}, 'FOSUserBundle') }}" name="_password" required="required">
                        </div>
                        <div class="form-group">
                                        <input type="checkbox" id="remember_me" name="_remember_me" value="on"/>
                                        {{ 'security.login.remember_me'|trans({}, 'FOSUserBundle') }}
                        </div>

                        <div class="form-group">
                            <a href="{{ path('fos_user_resetting_request') }}"><small>{{ 'forgotten_password'|trans({}, 'SonataUserBundle') }}</small></a>
                        </div>

                        <input type="submit" id="_submit" name="_submit" class="btn btn-primary block full-width m-b"
                        value="{{ 'security.login.submit'|trans({}, 'FOSUserBundle') }}"/>

                        <p class="text-muted text-center">
                            <small>Do not have an account?</small>
                        </p>
                        <a class="btn btn-sm btn-white btn-block" href="{{ path("sonata_user_registration_register") }}">Create an account</a>
                    </form>
                    <p class="m-t">
                        <small>Hugo Lehoux &copy; 2014</small>
                    </p>
                </div>
            </div>
        </div>
        <hr/>
        <div class="row">
            <div class="col-md-6">
                Hugo Lehoux
            </div>
            <div class="col-md-6 text-right">
               <small>© 2014-2015</small>
           </div>
       </div>
   </div>

   {% block javascripts %}
    {% javascripts
    '@ADMCoreBundle/Resources/public/js/jquery-2.1.1.js'
    '@ADMCoreBundle/Resources/public/js/bootstrap.min.js'
    '@ADMCoreBundle/Resources/public/js/inspinia.js'
    '@ADMCoreBundle/Resources/public/js/plugins/jquery-ui/jquery-ui.min.js'
    output='js/compiled/register.js'
    %}
    <script type="text/javascript" src="{{ asset_url }}"></script>
    {% endjavascripts %}
{% endblock %}
</body>
</html>

